# دليل النشر: الفرونت اند على Netlify + الباك اند على Render

المشروع مقسوم لجزئين ينشران بشكل منفصل تماماً:
- **`frontend/`** → يُنشر على Netlify (ملفات ستاتيك بس).
- **`backend/`** → يُنشر على منصة تدعم سيرفر Node.js دائم (هون بنستخدم Render كمثال، بس أي بديل مشابه زي Railway أو Fly.io بينفع بنفس الخطوات تقريباً).

**لماذا منفصلين؟** Netlify مش سيرفر تقليدي — هي استضافة صفحات ستاتيك، وما بتقدر تشغّل سيرفر Express دائم ولا تخزّن قاعدة بيانات SQLite بشكل ثابت. راجع الشرح الكامل بالمحادثة إذا حاب تفاصيل أكتر.

---

## الخطوة ١: انشر الباك اند أولاً (Render)

1. روح لـ [render.com](https://render.com) واعمل حساب (فيه خطة مجانية تكفي البداية).
2. **New → Web Service** واربطه بمستودع الـ Git تبعك، أو ارفع مجلد `backend/` مباشرة.
3. إعدادات الخدمة:
   - **Root Directory:** `backend`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
4. من تبويب **Environment**، ضيف هاي المتغيرات:
   ```
   NODE_ENV=production
   SESSION_SECRET=<قيمة عشوائية طويلة — ولّدها بالأمر تحت>
   CLIENT_URL=https://REPLACE-ME.netlify.app
   DB_PATH=./data/huda.db
   ```
   لتوليد `SESSION_SECRET` قوي، شغّل محلياً:
   ```bash
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```
5. **مهم بخصوص SQLite:** Render (وأي منصة serverless/ephemeral مشابهة) ما بتضمن إن ملف `huda.db` يضل موجود بعد كل إعادة نشر أو إعادة تشغيل، إلا إذا فعّلت **Persistent Disk** (متوفرة بخطط Render المدفوعة). لو ضليت على الخطة المجانية بدون قرص دائم، بيانات المستخدمين ممكن تنمسح مع أي إعادة نشر — هاي نقطة لازم تاخدها بعين الاعتبار قبل ما تعتمد عليها بالإنتاج الفعلي.
6. بعد النشر، Render رح يعطيك رابط زي: `https://huda-backend.onrender.com`. احتفظ فيه، رح تحتاجه بالخطوة الجاية.

---

## الخطوة ٢: اربط الفرونت اند بالباك اند

افتح `frontend/js/config.js` وبدّل السطر:

```js
const PRODUCTION_API_BASE_URL = 'https://REPLACE-WITH-YOUR-BACKEND-URL.onrender.com/api';
```

خليه رابط الباك اند الفعلي من الخطوة السابقة (بدون نسيان `/api` بالآخر).

---

## الخطوة ٣: انشر الفرونت اند (Netlify)

1. روح لـ [netlify.com](https://netlify.com) واعمل **Add new site → Import an existing project**.
2. اربطه بنفس مستودع الـ Git. `netlify.toml` الموجود بجذر المشروع رح يضبط كل شي تلقائياً (`base = "frontend"`, `publish = "."`) — يعني Netlify رح ينشر مجلد `frontend/` بس، ومجلد `backend/` ما رح يظهر ولا ينرفع إطلاقاً.
3. اضغط **Deploy**. بعد ما يخلص، رح تاخد رابط زي `https://huda-app.netlify.app`.

---

## الخطوة ٤: أكمل ربط الاتجاهين (CORS + الكوكيز)

هلأ عندك رابط Netlify الحقيقي. ارجع لإعدادات Render وحدّث المتغير:

```
CLIENT_URL=https://huda-app.netlify.app
```

(لو حاب تدعم روابط "Deploy Preview" المؤقتة اللي بتعطيها Netlify لكل Pull Request، ضيفها مفصولة بفاصلة بدون مسافة:
`CLIENT_URL=https://huda-app.netlify.app,https://deploy-preview-3--huda-app.netlify.app`)

بعد أي تعديل على متغيرات البيئة، لازم تعيد تشغيل خدمة الباك اند من Render.

---

## الخطوة ٥: جرّب

افتح رابط Netlify، جرّب تسجيل حساب جديد، سجّل خروج، سجّل دخول مرة ثانية، وتأكد إن التقدم (تسبيح، حفظ، قراءة) بيضل محفوظ بعد إعادة تحميل الصفحة. لو ظهرت مشكلة بتسجيل الدخول تحديداً (الجلسة ما بتثبت)، افتح Developer Tools → Network وتأكد إن كوكي `connect.sid` عم ينرجع بالـ response وإن ماكو خطأ CORS بالـ Console.

---

## الخطوة ٦: فعّل إشعارات الأذان الحقيقية (Web Push) — تصل حتى لو التطبيق مغلق

هاي ميزة إضافية فوق النشر العادي، تحتاج خطوتين بس:

1. **ولّد مفاتيح VAPID خاصة بمشروعك** (لا تستخدم أي مفتاح تجريبي موجود بالكود محلياً بالإنتاج):
   ```bash
   node backend/scripts/generate-vapid-keys.js
   ```
   رح يطلعلك `VAPID_PUBLIC_KEY` و `VAPID_PRIVATE_KEY`.

2. **أضفهم كمتغيرات بيئة على Render** (نفس مكان `SESSION_SECRET`):
   ```
   VAPID_PUBLIC_KEY=...
   VAPID_PRIVATE_KEY=...
   VAPID_SUBJECT=mailto:بريدك@example.com
   ```
   وأعد تشغيل الخدمة.

هيك بس — الفرونت اند بيجيب المفتاح العام تلقائياً من `/api/push/vapid-public-key`، ما في شي لازم تعدّله بملفات الفرونت اند.

**⚠️ ملاحظتين مهمتين قبل ما تعتمد عليها فعلياً:**

- **السيرفر لازم يضل شغّال بشكل دائم (Always-On).** المجدول (`backend/lib/prayer-push-scheduler.js`) يفحص كل دقيقة إذا دخل وقت صلاة لأي مشترك ويبعت الإشعار. خطة Render المجانية توقف الخدمة تلقائياً بعد ~15 دقيقة من عدم الاستخدام، وبهالحالة المجدول بيتوقف وبتفوت إشعارات لحد ما توصل زيارة جديدة توقظ السيرفر. الحلول:
  - رقّي لخطة مدفوعة (Starter فما فوق) بتضمن Always-On، أو
  - استخدم خدمة "keep-alive" مجانية (مثل UptimeRobot أو cron-job.org) تعمل ping لـ `/api/health` كل ٥-١٠ دقائق عشان يضل السيرفر صاحي (هاد حل مؤقت وغير مضمون ١٠٠٪، بس بيقلل المشكلة كثير).
- **لا تولّد مفاتيح VAPID جديدة بعد ما يشترك مستخدمون حقيقيون** — أي تغيير بالمفاتيح بيبطل كل الاشتراكات القديمة تلقائياً، والمستخدمين لازم يعيدوا الضغط على "تفعيل الأذان التلقائي" من جديد.

---

## ملخص التغييرات اللي صارت بالكود عشان هاد كله يشتغل

- `backend/server.js`: كوكي الجلسة صارت `sameSite: 'none'` بالإنتاج (لازم لأن الفرونت اند والباك اند على نطاقين مختلفين)، و CORS صار يقبل أكثر من نطاق (Netlify + deploy previews) بدل نطاق واحد فقط.
- `frontend/js/config.js`: رابط الـ API صار يتحدد تلقائياً — `/api` محلياً، ورابط الباك اند الكامل بأي نطاق ثاني.
- المشروع انقسم لمجلدين `frontend/` و `backend/` بدل ما يكونوا مختلطين، عشان Netlify ينشر الفرونت اند بس ولا يعرّض كود أو بيانات الباك اند للعامة.
- `netlify.toml` و `.gitignore` جداد لضبط النشر وحماية الأسرار (`.env`، ملف قاعدة البيانات) من الرفع عن طريق الخطأ.
